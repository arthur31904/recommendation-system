# from database import Engine
# from models import brand_model, brand_log_model, website_model, brand_news_model, point_rule_model, service_model, tag_model
#
# tag_model.Base.metadata.create_all(bind=Engine)
# service_model.Base.metadata.create_all(bind=Engine)
# brand_model.Base.metadata.create_all(bind=Engine)
# brand_log_model.Base.metadata.create_all(bind=Engine)
# brand_news_model.Base.metadata.create_all(bind=Engine)
# point_rule_model.Base.metadata.create_all(bind=Engine)
# website_model.Base.metadata.create_all(bind=Engine)